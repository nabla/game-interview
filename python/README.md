# game-interview - Python

Start by creating a virtual environment:
```
python3 -m venv .venv
source .venv/bin/activate
```

Install the packages:
```
pip install -r requirements.txt
```

Run the game:
```
python3 main.py
```